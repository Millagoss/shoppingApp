import styled from 'styled-components';

export const AsideContainer = styled.aside`
  margin-top: 20px;
  display: flex;
  width: inherit;

  p {
    width: auto;
    margin: 2rem auto 0 auto;
  }
`;
